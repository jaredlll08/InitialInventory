{
  "schemaVersion": 1,
  "id": "${MODID}",
  "name": "${NAME}",
  "version": "${version}",
  "description": "${DESCRIPTION}",
  "authors": [
    "${AUTHOR}"
  ],
  "contact": {
    "homepage": "${CURSE_HOMEPAGE}",
    "sources": "${GIT_REPO}",
    "issues": "${GIT_REPO}/issues"
  },
  "license": "MIT",
  "icon": "icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.blamejared.initialinventory.InitialInventoryFabric"
    ]
  },
  "mixins": [
    "initialinventory.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${FABRIC_LOADER}",
    "java": ">=${JAVA}",
    "minecraft": "${MINECRAFT}",
    "fabric": "*",
    "crafttweaker": ">=${CRAFTTWEAKER}",
    "faux-custom-entity-data": ">=${FAUX_CUSTOM_ENTITY_DATA}"
  }
}
  